meta {
  name: Get All Jobs
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/jobs
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("should return 200", function() {
    expect(res.status).to.equal(200);
  });
}
